pop(); //removes last val of array
push();//adds val to the end of array
shift(); //removes first value and shifts the rest up
unshift(); //adds one or more elements to the start of the array
//can take paramters, which will be inserted to the start 
toLowerCase(); //converts a string to lowercase letters.
toUpperCase(); //converts a string to uppercase letters.
split(); //separates with comma
slice(start,end); //extracts parts of a string and returns the extracted parts in a new string.
//start is required (where to begin extraction), end optional